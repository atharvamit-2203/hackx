services:
  - type: web
    name: plugmind-backend
    runtime: python
    plan: free
    rootDir: Backend
    buildCommand: "pip install requests fastapi uvicorn python-multipart python-dotenv pymongo firebase-admin pyyaml joblib numpy"
    startCommand: "uvicorn render_app:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: PORT
        value: "10000"
    healthCheckPath: "/health"
    autoDeploy: true
